# Training pipeline dependencies.
# This environment is for your machine / Colab only — it is never deployed.
# The API deliberately does NOT install torch; see api/requirements.txt.

torch>=2.9,<3
numpy>=2.0,<3

# Landmark extraction from video.
mediapipe>=1.0,<2
opencv-python>=4.10,<6

# Export the trained checkpoint to ONNX, and sanity-check it before shipping.
onnx>=1.19,<2
onnxruntime>=1.23,<2
# Required by torch>=2.9's ONNX exporter. Not a transitive dependency of torch —
# omit it and `torch.onnx.export` fails with ModuleNotFoundError at export time.
onnxscript>=0.5

# Training loop odds and ends.
pyyaml>=6.0
tqdm>=4.66
scikit-learn>=1.5          # confusion matrices and split stratification

# Tests.
pytest>=8.0
